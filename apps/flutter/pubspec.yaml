name: appointment_app
description: 预约服务系统 - 用户端APP
version: 1.0.0

environment:
  sdk: '>=3.2.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  dio: ^5.4.0
  get: ^4.6.6
  shared_preferences: ^2.2.2
  cached_network_image: ^3.3.1
  flutter_easyloading: ^3.0.5
  pull_to_refresh: ^2.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
