# شرح الميزات
> **Languages**: [中文](../FEATURES.md) · [English](../en/FEATURES.md) · [한국어](../ko/FEATURES.md) · [Русский](../ru/FEATURES.md) · [Deutsch](../de/FEATURES.md) · [Français](../fr/FEATURES.md) · [Español](../es/FEATURES.md) · [Português](../pt/FEATURES.md) · [हिन्दी](../hi/FEATURES.md) · [বাংলা](../bn/FEATURES.md) · [Bahasa Indonesia](../id/FEATURES.md) · [日本語](../ja/FEATURES.md)

> **حالة المشروع**: مكتمل بالكامل ✅ | 109 وحدة تحكم | 103 نماذج | 344 اختبارًا (service 240 / admin 104) | WebSocket | استدعاءات الدفع | أرقام الطابور | التقييم | المجتمع

## أولًا: واجهة المستخدم (برنامج WeChat الصغير + تطبيق Flutter)

وظائف البرنامج الصغير والتطبيق متطابقة تمامًا. الحساب الموحد يدعم تبديل هوية العميل/الفني.

### 1. المصادقة

| الميزة | الوصف |
|------|------|
| التسجيل برقم الهاتف | رقم الهاتف + رمز التحقق + كلمة المرور + تأكيد كلمة المرور، يدعم رمز التوصية |
| تسجيل الدخول بكلمة المرور | رقم الهاتف المسجل + كلمة المرور |
| تسجيل الدخول برمز التحقق | رقم الهاتف المسجل + رمز التحقق |
| تسجيل الدخول عبر WeChat | تفويض تسجيل الدخول عبر WeChat، يتطلب ربط رقم الهاتف أول مرة |
| وضع الضيف | يمكن التصفح دون تقديم طلبات، التقديم يتطلب التسجيل |
| نسيت كلمة المرور | تعديل كلمة المرور عبر رمز التحقق |
| اتفاقية المستخدم/اتفاقية الخصوصية | قابلة للتحرير في لوحة الإدارة، تُعرض عند التسجيل |

### 2. الصفحة الرئيسية

| الميزة | الوصف |
|------|------|
| تحديد المواقع LBS | تحديد موقع المنطقة، عرض خدمات المنطقة، دعم تبديل المدينة |
| الشرائح الدوارة | تلقائية، إعدادات الانتقال في لوحة الإدارة (صفحة ويب/تفاصيل/بدون إجراء) |
| الإعلانات | تمرير تلقائي، نقر لعرض القائمة، تُضاف من لوحة الإدارة |
| تصنيفات الخدمات | صورة/اسم/سعر/مبيعات، نقر للدخول للتفاصيل |
| كوبون المستخدم الجديد | الحصول التلقائي عند التسجيل |

### 3. الخدمات

| الميزة | الوصف |
|------|------|
| المعلومات الأساسية | صورة/اسم/سعر/مبيعات/مواصفات/مدة الخدمة/تفاصيل الخدمة |
| تقييمات المستخدمين | عرض محتوى التقييم، يمكن عرض المزيد |
| حجز الخدمة | الدخول إلى صفحة تأكيد الطلب |
| اختيار المتجر | عنوان متجر الخدمة (ملاحة)/ساعات العمل/رقم الهاتف |
| اختيار الفني | اسم الفني/الصورة/الدرجة |
| وقت الخدمة | اختيار فترة الحجز |
| خصم 9% لفترات الذروة المنخفضة | 10-12 / 17-18 / بعد 21:00 |
| خصم 95% للحجز المبكر | قبل 30 دقيقة، لا يتراكم مع الكوبونات |
| الكوبونات | عرض المبلغ المتاح، استخدام/عدم استخدام |
| الملاحظات | ملاحظات متطلبات الخدمة (حد عدد الأحرف) |
| اتفاقية الخدمة | قراءة وتأكيد قبل التقديم |

### 4. بحث المنتجات وسلة التسوق

| الميزة | الوصف |
|------|------|
| بحث المنتجات | بحث بالاسم |
| تصفية الفئات | البحث حسب التصنيف |
| تفاصيل المنتج | كمية الشراء/المفضلة/المشاركة/الإضافة للسلة/شراء فوري |
| سلة التسوق | اختيار/حذف/تعديل الكمية |

### 5. الطلبات

| الميزة | الوصف |
|------|------|
| جميع الطلبات | عرض حسب تبويبات الحالة |
| بانتظار الدفع | عرض/دفع |
| بانتظار الشحن/الاستلام الذاتي | طلب الشحن/إلغاء الطلب/عرض |
| بانتظار الاستلام | معلومات الشحن/تأكيد الاستلام |
| بانتظار التقييم | تفاصيل الطلب/تقييم نصي + صور |
| مكتمل | عرض معلومات الطلب |
| قواعد الاسترداد | خلال 15 دقيقة من الطلب أو >6 ساعات استرداد 100% / <6 ساعات 90% / بعد البدء 80% / بعد التأكيد لا استرداد |

### 6. الفنيون (من منظور العميل)

| الميزة | الوصف |
|------|------|
| قائمة الفنيين | من الأقرب للأبعد/الصورة/الاسم/عدد الطلبات/الدرجة/المفضلة/المسافة/الأوقات المتاحة/حجز فوري |
| تفاصيل الفني | صورة/اسم/مسافة/طلبات/تقييمات/مفضلة/قائمة الخدمات المتاحة |
| انضمام الفنيين | تعبئة المعلومات للتقدم ليصبح فنيًا، تنزيل تطبيق الفني |

### 7. لوحة عمل الفني (بعد تبديل هوية الفني)

| الميزة | الوصف |
|------|------|
| نظرة اليوم | نظرة عامة على طلبات/دخل اليوم |
| إعداد الجدولة | تحديد الفترات الزمنية القابلة للحجز يوميًا |
| طلباتي | المحجوزة غير المُتحقق منها/المكتملة |
| التحقق بالمسح | مسح رمز المستخدم للتحقق من المرات |
| إدارة العضويات | قائمة العملاء المخدومين/بيانات الاستهلاك/بطاقات المرات/تعديل الملف |
| إدارة الأرباح | دخل اليوم/قيد التسوية/رصيد المحفظة |
| الأموال في الطريق | تم التحقق لم يُسوَّ بعد، تأكيد تلقائي خلال 3 أيام |
| السحب | في اليوم 20 من كل شهر، وصول T+1 إلى WeChat؛ مراجعة في لوحة الإدارة، مبالغ ≥500 بموافقة مستويين (مدير المتجر←المالية)؛ حجز مسبق في الطريق عند التقديم، إعادة فحص قبل تحويل الموافقة، منع الدفع المزدوج في الموافقات المتزامنة (تقوية 2026-08-26) |
| الحضور | تسجيل الوصول/المغادرة/رفع صور النظافة |
| مكافأة العميل العائد | تسجيل جائزة الاستهلاك الثانية خلال 30 يومًا |
| التدريب المهني | دورات فيديو/دورات مصورة |
| مهام اليوم | WorkController today: جلب المهام المعلقة في الوقت الفعلي |
| سجل الإنجاز | WorkController records: سجل الإنجازات السابق |
| بدء/إتمام الخدمة | WorkController start/complete: قفل صفوف + حارس آلة الحالة + ذرّية، كتابة إشعار داخلي تلقائيًا بعد الإتمام |
| لوحة عمل الفني في البرنامج الصغير | ثلاثة تبويبات tech-work: التحقق بالمسح/مهام اليوم/سجل الإنجاز |

### 8. الملف الشخصي

| الميزة | الوصف |
|------|------|
| المعلومات الشخصية | الصورة/الاسم المستعار/رقم الهاتف |
| تبديل الهوية | العميل ↔ الفني |
| إشعارات الرسائل | الإشعارات الداخلية (appointment_notification)؛ صفحة مركز الرسائل: ترقيم صفحات/سحب للتحديث/تمييز المقروء/تعليم كمقروء/تعليم الكل كمقروء |
| بطاقات عضويتي | شهرية/سنوية VIP/بطاقة مرات (الانتهاء/المرات/المستخدم/المتبقي) |
| نقاطي | سجل الحصول/النقاط المتاحة/سجل الاستخدام (1:100 صرف بطاقة هدايا)؛ نقاط الحضور/الاستهلاك، استرداد نسبي عند الاسترداد، سجل مفصل + تصفية type/source |
| بطاقات هداياي | بطاقة نقدية/هدية عينية؛ نوع cash عند الصرف يُشحن مباشرة إلى المحفظة |
| الكوبونات | المستلمة المتاحة/المستخدمة/المنتهية |
| مفضلتي | الخدمات المفضلة |
| متابعة الحساب الرسمي | نافذة رمز QR منبثقة، ضغط مطول للحفظ |
| ترويج المستخدم | شرح الترويج/ملصق رمز QR/قائمة المستخدمين الموصى بهم/مكافآت النقاط |
| الملاحظات | نص + صور، رد خلال 24 ساعة |
| عنا | LOGO/مقدمة/هاتف خدمة العملاء/الموقع الرسمي/البريد |

### 9. الإعدادات

| الميزة | الوصف |
|------|------|
| تعديل كلمة المرور | كلمة المرور الحالية + الجديدة + تأكيد الجديدة |
| تغيير رقم الهاتف | رمز تحقق الهاتف الحالي + رمز تحقق الهاتف الجديد |
| اتفاقية المستخدم | عرض نصي، قابلة للتحرير في الخلفية |
| اتفاقية الخصوصية | عرض نصي، قابلة للتحرير في الخلفية |
| فحص التحديث | رقم الإصدار + التحديث |
| حذف الحساب | شرح الحذف + تأكيد العملية |
| تسجيل الخروج | مسح حالة تسجيل الدخول |

### 10. محفظة الشحن (الجولة 6)

| الميزة | الوصف |
|------|------|
| رصيد المحفظة | GET /api/wallet الرصيد + السجل (جداول user_wallet/wallet_recharge/wallet_txn) |
| الشحن | POST /api/wallet/recharge إنشاء طلب شحن؛ POST /api/wallet/recharge/{id}/pay دفع WeChat، الاستدعاء ببادئة R |
| الدفع بالرصيد | قناة دفع الطلب pay_channel=balance |
| إعادة شحن الاسترداد | استرداد WeChat/الرصيد يعيد الشحن تلقائيًا (refundToBalance / creditRefundToWallet) |

### 11. رسائل الاشتراك (الجولتان 6+8)

| الميزة | الوصف |
|------|------|
| سيناريوهات الاشتراك | 3 سيناريوهات أحداث الطلب: نجاح الدفع / وصول الاسترداد / نجاح التحقق |
| الذرّية | push_sent_at يمنع الدفع المكرر |
| التنازل | تنازل تلقائي إلى إشعارات داخلية عند عدم إعداد قالب الاشتراك |

### 12. إغلاق حلقة بطاقة المرات (الجولة 8)

| الميزة | الوصف |
|------|------|
| بطاقات مراتي | GET /api/marketing/cards/my حساب used_up/expired في الوقت الفعلي |
| التحقق وخصم المرات | POST /api/marketing/cards/use: Redis NX ذرّي + lockForUpdate قفل صفوف، إنشاء مباشر لطلب completed + OrderItem + OrderPayment(pay_type='card') |

### 13. خصم الكوبون (الجولة 9)

| الميزة | الوصف |
|------|------|
| اختيار الكوبون عند الطلب | يمكن تمرير user_coupon_id، PriceCalculator.applyCoupon تحقق قراءة + حساب |
| أنواع الخصم | fixed مبلغ ثابت / percent نسبة مئوية، حد min_amount |
| الاستهلاك والإرجاع | consume عند نجاح الدفع يضع used؛ الاسترداد restoreCouponAndCard إرجاع ذرّي |

### 14. بطاقة الهدايا (الجولة 9)

| الميزة | الوصف |
|------|------|
| الصرف | redeem: نوع cash يُشحن إلى المحفظة (قفل صفوف ضد الإيداع المزدوج، WalletTxn type='gift_card')، نوع gift يُعلّم فقط |
| بطاقات هداياي | GET /api/marketing/gift-cards/my |

### 15. نظام النقاط (الجولتان 9+10)

| الميزة | الوصف |
|------|------|
| نقاط الحضور | CheckIn حضور يومي |
| نقاط الاستهلاك | عند التحقق floor(paid×1)، ذرّية عبر order_id، لقطة balance |
| استرداد النقاط | clawbackOrderPoints استرداد نسبي (3 نقاط ربط) |
| النقاط نقدًا | تمرير use_points عند الدفع، 100 نقطة = 1 يوان (config app.points_rate)، تحقق SUM مجمع للرصيد، سجل الاستهلاك source=points_offset ذرّي |
| إعادة النقاط (الجولة 15) | إلغاء/استرداد يعيد نقاط points_offset: refundOffsetPoints 5 نقاط ربط (doCancel 3 مسارات/doRefund معاملة WeChat/creditRefundToWallet/completeOneRefundCompensation)، source=points_refund ذرّي |
| تفاصيل النقاط | GET /api/marketing/points ترقيم صفحات + تصفية type/source، type موحد كـ earn |

### 16. سلسلة طلب البرنامج الصغير (الجولة 10)

| الميزة | الوصف |
|------|------|
| صفحة تفاصيل الخدمة | service/detail |
| صفحة تأكيد الطلب | order/confirm: اختيار الكوبون/تعطيل الحد/تقدير السعر في العميل ← POST /order ← دفع WeChat/الرصيد |
| حجم الصفحات | البرنامج الصغير الآن 20 صفحة |

### 17. ثلاثة مداخل في واجهة المستخدم (الجولة 10)

| الميزة | الوصف |
|------|------|
| المفضلة | صفحة favorite المفضلة (مدخل صفحة user) |
| الترويج | referral: رمز الدعوة/نسخ الرابط/قائمة المستخدمين الموصى بهم |
| الملاحظات | نموذج feedback |

### 18. تفويض رسائل الاشتراك (الجولة 14)

| الميزة | الوصف |
|------|------|
| تفويض الاشتراك | utils/subscribe.js إدارة مركزية لمعرّفات القوالب (أسماء المفاتيح محاذاة مع appointment_system_config.wechat_app.template_ids في الخادم) |
| سيناريوهات التشغيل | wx.requestSubscribeMessage داخل استدعاءات الإيماءات بعد نجاح الحجز/الدفع، صامت عند عدم إعداد معرّف القالب أو رفض المستخدم |
| سلسلة الخادم | WechatTemplateMessageService إرسال + NotificationReminderService تذكير قبل الحجز بساعتين إلى ساعة + عملية فحص AutoCancelTimer |

### 19. ما بعد البيع واستبدال البضائع (الجولة 14)

| الميزة | الوصف |
|------|------|
| تقديم ما بعد البيع | POST /api/aftersales: type=refund/exchange، تحقق طلب المستخدم/paid+completed/إزالة تكرار نفس الطلب |
| ما بعد بيعي | GET /api/aftersales قائمة مرقمة + GET /api/aftersales/{id} تفاصيل |
| تدفق المراجعة | approve/reject في لوحة الإدارة (rejected يلزم remark)؛ approved مجرد انتقال حالة، الاسترداد يستخدم واجهة استرداد الطلب |

### 20. الشراء الجماعي/الفلاش (الجولة 15)

> منذ 2026-08 أُلغيت قناة FLASH_SALE: PromotionController::index يفلتر flash_sale، show/join يعيدان لها 400، الفلاش موحد عبر قناة «43. الفلاش (الجولة 24)»؛ ثابت `Promotion::TYPE_FLASH_SALE` محفوظ للتوافق مع البيانات التاريخية. هذا القسم و«27. طلب الفلاش» سجلات تاريخية.

| الميزة | الوصف |
|------|------|
| قائمة/تفاصيل الأنشطة | GET /api/promotions + /api/promotions/{id}، تصفية نوع group_buy/flash_sale |
| المشاركة | POST /api/promotions/join/{id}: قفل Redis NX ضد البيع الزائد (flash_sale بسقف max_people)، مشاركة مكررة 422، قفل الامتلاء group_buy، إغلاق كسول عند عدم الامتلاء قبل الانتهاء (status=0 عند show/join) |
| قائمة المشاركين | GET /api/promotions/{id}/participants |
| إصلاح الحالة | حالة PromotionParticipant تحولت إلى ثوابت أعداد صحيحة 0/1/2/3 (إصلاح تلف join 1366 في الوضع الصارم) |

### 21. طلب الشراء الجماعي (الجولة 16)

| الميزة | الوصف |
|------|------|
| سعر المجموعة | استجابة join تعيد discount_percent/original_price/group_price |
| طلب المجموعة | POST /api/order مع promotion_id: تحقق group_buy فقط/نشاط سارٍ/المتصل مشارك/غير ممتلئ/تطابق الخدمة؛ سعر المجموعة = السعر الأصلي × discount_percent/100، تعطيل تراكب الكوبون/بطاقة المرات/النقاط (422) |
| علامة الطلب | appointment_order يضيف عمودي promotion_id/participant_id + فهارس |
| معالجة عدم التكوين | عدم الامتلاء عند الانتهاء ← إغلاق النشاط + إلغاء جماعي لطلباته pending (ذرّي)؛ pay() فحص كسول: إذا أُغلق يُلغى الطلب تلقائيًا وتُحرر أقفال الفني |

### 22. عمولة الإحالة (الجولة 16)

| الميزة | الوصف |
|------|------|
| قاعدة الإصدار | بعد إتمام أول طلب للمُحال: المبلغ = paid_amount × reward_rate (appointment_system_config referral.reward_rate افتراضي 0.05، تراجع إلى ثابت عند القيمة غير الصالحة)، يُصدر فقط عند >0 |
| نقطة الربط | ReferralRewardService::handleOrderCompleted مرتبط داخل معاملة WorkController::complete (المدخل الوحيد serving→completed، تحقق verify يصل فقط إلى serving دون تشغيل)، فشل كامل يُرجع ويُعاد |
| الذرّية | قفل صفوف appointment_user_referral lockForUpdate + فحص rewarded_at فارغ + إعادة فحص أول طلب داخل القفل (متزامن/مكرر يصدر مرة واحدة فقط) |
| الإيداع | تراكم قفل صفوف المحفظة + WalletTxn type='referral_reward' (balance_after + ملاحظة رقم الطلب)؛ سجل الإحالة يكتب reward_type/reward_amount/rewarded_at/first_order_at |
| التفاصيل | GET /api/user/referral/earnings ترقيم صفحات (اسم مستعار المُحال/صورة/رقم الطلب/المبلغ/الوقت) |

### 23. متجر صرف النقاط (الجولة 16)

| الميزة | الوصف |
|------|------|
| منتجات الصرف | appointment_points_exchange_goods: type=coupon/gift_card/wallet، points_cost/value (DECIMAL(25,2) ضد فقدان دقة معرف الثلج)/stock/status |
| قائمة المنتجات | GET /api/marketing/points-exchange: منتجات مُعلنة + مخزون متبقٍ حي + عدد المُصرف |
| الصرف | POST /api/marketing/points-exchange/{id}: قفل Redis NX + قفل صفوف المنتج ضد الإفراط؛ تحقق SUM نقاط (ناقص 422) + UserPoints type='consume' source='exchange' خصم؛ coupon إصدار كوبون / wallet إيداع رصيد (WalletTxn points_exchange) / gift_card إرجاع كلمة المرور |
| الذرّية | فهرس فريد uk_user_goods حد مرة واحدة لنفس المستخدم/المنتج + إعادة فحص داخل القفل + احتياطي 1062؛ سجل صرف appointment_user_points_exchange |

### 24. إعادة جدولة الحجز (الجولة 17)

| الميزة | الوصف |
|------|------|
| الواجهة | POST /api/order/reschedule/{id}: new_service_time (إلزامي) + reason (اختياري)، تغيير الوقت مع نفس الفني |
| القواعد | طلب المستخدم فقط (غير المالك 404)؛ نوع appointment فقط وحالة pending/paid/confirmed (البقية 422)؛ ≥ 6 ساعات من بدء الخدمة الأصلية (متوافق مع نافذة الاسترداد الكامل) |
| الحماية من التزامن | B1 order_lock (نفس عائلة الاستبعاد مع pay/cancel/refund) ← قفل فترة جديدة للفني Redis SETNX EX 180 (منع البيع الزائد في التعديل المتزامن) ← إعادة قراءة بقفل صفوف داخل المعاملة + تحقق B2 تعارض جدولة DB (استبعاد هذا الطلب) |
| الإنهاء | تحديث service_time + كتابة appointment_order_reschedule (مع reason) + تحرير قفل الفترة الأصلية/الجديدة الذي يحمله هذا الطلب؛ فشل المعاملة يرجع ويحرر قفل الفترة الجديدة |
| الإشعار | رسالة اشتراك SCENE_RESCHEDULE (تنازل إلى إشعار داخلي «نجحت إعادة جدولة الحجز» عند عدم إعداد القالب) + pushOrderUpdate |

### 25. إهداء الكوبونات (الجولة 17)

| الميزة | الوصف |
|------|------|
| الواجهة | POST /api/marketing/coupons/transfer (user_coupon_id) توليد رمز إهداء فريد 8 خانات (uk_code احتياطي، صالح 7 أيام)؛ POST /api/marketing/coupons/claim (code) استلام؛ GET /api/marketing/coupons/transfers المرسل (pending/claimed/expired) + المستلم (claimed) مرقّم |
| التحقق | الكوبون يخص المستخدم/available/تعريف الكوبون غير منتهي/لم يُهدَ من قبل (422)؛ لا يمكن استلام كوبون هداه الشخص نفسه، المستلم ليس المالك الأصلي |
| ضد سوء الاستخدام | قفل Redis NX coupon_transfer_claim:{code} (30 ثانية) + إعادة فحص بقفل صفوف داخل المعاملة ضد الإنفاق المزدوج؛ فهرس فريد uk_user_coupon حد إهداء مرة واحدة لنفس الكوبون؛ الكوبون المهدى لا يُهدى مجددًا (الكوبون الجديد بلا سجل إهداء يُمنع طبيعيًا)؛ انتهاء كسول يضع expired + استعادة الكوبون الأصلي available |
| الاستلام | داخل المعاملة: الكوبون الأصلي يُوضع used + إنشاء UserCoupon جديد مربوط بالمستلم (coupon_id دون تغيير أي صلاحيته دون تغيير) + سجل الإهداء يُوضع claimed |

### 26. انتهاء النقاط (الجولة 17)

| الميزة | الوصف |
|------|------|
| الصلاحية | عمود appointment_user_points.expires_at؛ جميع earn (حضور/استهلاك/إعادة) تُكتب مع expires_at = now + points.expiry_days (افتراضي 365، ≤0 لا تنتهي أبدًا)؛ consume/use فارغة |
| تنفيذ الانتهاء | عملية PointsExpiryTimer كل 60 ثانية مسح مؤشر (100/دفعة) لصفوف earn التي expires_at < now ← كتابة صف خصم سالب type=expire (source=expiry + order_id لأثر السجل الأصلي) ← إشعار داخلي مجمع «انتهت صلاحية X نقطة لديك» |
| الذرّية | ① صف expire يشير order_id إلى سجل earn الأصلي، داخل المعاملة lockForUpdate + إعادة فحص exists (العمليات المتزامنة تتسلسل على قفل الصف) ② ترقيم مؤشر id ③ الإشعار فقط في جولة الخصم الفعلية |
| المعيار | الرصيد المتاح SUM مجمع يشمل صفوف expire السالبة؛ النقاط المنتهية لا تُستخدم نقدًا/صرفًا |

### 27. طلب الفلاش (الجولة 18، أُلغيت)

> استُبدلت بقناة `/api/seckill` للجولة 24 (فرع الترويج في store() بقي للشراء الجماعي فقط)، انظر «43. الفلاش».

| الميزة | الوصف |
|------|------|
| الواجهة | POST /api/order مع promotion_id (نوع flash_sale): سعر الفلاش = round(total × (100 − discount_percent)/100, 2)، متسق مع معيار سعر فلاش PromotionController |
| التحقق | قائمة بيضاء أنواع [group_buy, flash_sale] (البقية 422)؛ نشاط جارٍ؛ المتصل مشارك؛ تطابق خدمة الطلب مع النشاط؛ نفاد participants_count ≥ max_people 422 «بُيع بسرعة»؛ تعطيل تراكب الكوبون/بطاقة المرات/النقاط 422 |
| الانتهاء | pay() فحص كسول isFlashSaleClosed (بنمط isGroupBuyClosed): انتهاء الفلاش ← النشاط 0 + إلغاء جماعي لطلباته pending + إلغاء تلقائي لهذا الطلب + تحرير قفل الفني 422 |

### 28. تذكير الخدمة + تذكير الانتهاء (الجولة 18)

| الميزة | الوصف |
|------|------|
| تذكير قبل بدء الخدمة | ServiceReminderTimer مسح 60 ثانية لطلبات service_time ∈ [now+1h, now+1h+60s) وحالة confirmed/serving ونوع appointment ← إشعار داخلي (type='service_reminder'، مع الخدمة/الفني/المتجر/الوقت) + رسالة اشتراك SCENE_REMINDER |
| تذكير الانتهاء | ExpiryReminderTimer مسح 6 ساعات لـ end_at ∈ (now, now+3d+6h]: عضويات active (type='card_expiry') + كوبونات available (type='coupon_expiry'، whereHas تعريف الكوبون المرتبط end_at) + رسالة اشتراك SCENE_EXPIRY |
| الذرّية | كلاهما مؤشر id 100/دفعة + إعادة فحص بقفل صفوف داخل المعاملة + إزالة تكرار إشعار (عمود order_id يسجل المصدر id/معرّف الطلب كمفتاح منع تكرار)؛ كتابة push_sent_at فقط عند نجاح دفع رسالة الاشتراك، الفشل يعاد في الجولة التالية |
| التنازل | قالب غير مهيأ (WECHAT_SUBSCRIBE_TEMPLATE_REMINDER / _EXPIRY) تنازل تلقائي إلى إشعار داخلي فقط |

### 29. رد الفني على التقييم (الجولة 18)

| الميزة | الوصف |
|------|------|
| الواجهة | POST /api/technician/review/reply/{order_id} (وسيط هوية الفني): تقييم غير موجود/ليس للفني 404 موحد؛ رد موجود 422 (رفض ذرّي دون استبدال)؛ رد فارغ 422 |
| بعد الرد | إشعار داخلي للمستخدم (type='review_reply'، try/catch غير حاجب + Log) |
| البيانات | appointment_order_review إضافة ذرّية لعمود replied_at (عمود reply موجود من الإنشاء)؛ قوائم/تفاصيل التقييم في لوحة الإدارة تمرر reply/replied_at عبر decorate()->toArray() |

### 30. إشعار وصول الشحن (الجولة 18)

| الميزة | الوصف |
|------|------|
| الواجهة | استدعاء شحن WeChat (بادئة R) handleRechargeNotify داخل معاملة: بعد WalletTxn كتابة إشعار داخلي type='wallet_recharge'، «شحنت بنجاح ¥X.XX» (مبلغ يوان، number_format خانتان) |
| الذرّية | إعادة استخدام ذرّية الاستدعاء الحالية (lockForUpdate صف الشحن + إعادة فحص الحالة، فقط الانتقال الأول pending→paid يصل للإشعار)؛ الإشعار وتغيير الحالة يُرسلان ذريًا في نفس المعاملة دون فجوة انهيار؛ فشل التحقق/رقم غير موجود/مبلغ غير مطابق لا يكتب إشعارًا |
| تحمل الأخطاء | كتابة الإشعار try/catch، الفشل يسجل warning فقط دون إعاقة العملية الرئيسية |

### 31. تحويل الرصيد (الجولة 19)

| الميزة | الوصف |
|------|------|
| الواجهة | POST /api/wallet/transfer: فك ترميز hashid المستلم + وجود 404، تحويل لنفسه 422، مبلغ 0.01-1000/عملية 422 (مقارنة DECIMAL وليس float)، رصيد غير كافٍ 422، حد يومي تراكمي 5000 يوان 422 |
| التزامن/الذرّية | قفل Redis NX wallet_transfer:{from} 30 ثانية لتسلسل المرسل؛ داخل المعاملة lockForUpdate لصفوف المحفظة بترتيب تصاعدي user_id للطرفين (ترتيب ثابت ضد الجمود)؛ client_token بعد النجاح SETNX 24 ساعة ضد الإرسال المكرر (طلبات الفشل لا تُسجل token وتُعاد) |
| الإيداع | خصم المرسل + إضافة المستلم + سجلا WalletTxn (transfer_out/transfer_in مع لقطة balance_after) + سجل تحويل completed + إشعار داخلي للمستلم type='balance_received' (الفشل يسجل فقط) |
| السجلات | GET /api/wallet/transfers (direction=out/in مرقّم) + GET /transfers/{id} (مرئي للطرفين فقط 404) |

### 32. إهداء النقاط (الجولة 19)

| الميزة | الوصف |
|------|------|
| الواجهة | POST /api/user/points/transfer: المستلم موجود 404، إهداء لنفسه 422، عدد 1-10000 422، رصيد SUM غير كافٍ 422، حد يومي تراكمي 10000 422 |
| التزامن/الذرّية | قفل Redis NX points_transfer:{user} 30 ثانية؛ داخل المعاملة lockForUpdate لآخر سجل لكل طرف (ترتيب تصاعدي user_id ضد جمود التبادل) + إعادة فحص الرصيد/الحد/المستلم داخل القفل |
| معايير السجلات | المرسل type=consume source=points_transfer سالب (balance=لقطة السابق − هذه، نفس معيار points_offset/exchange)؛ المستلم type=earn source=points_transfer موجب مع expires_at (يمكن لـ PointsExpiryTimer انتهاؤه طبيعيًا)؛ كتابة سجل الإهداء داخل المعاملة، بعد commit إشعار داخلي للمستلم type='points_received' |
| السجلات | GET /api/user/points/transfers (direction=sent/received مرقّم، اسم الطرف الآخر) |

### 33. إضافة تقييم + استكمال مسار التقديم (الجولة 19)

| الميزة | الوصف |
|------|------|
| الإضافة | POST /api/order/review/{order_id}/append: تقييم غير موجود/ليس للفني 404 موحد، غير completed 422، إضافة مكررة 422 (append_content/append_at أي منها غير فارغ يرفض)، محتوى فارغ 422؛ بعد النجاح كتابة append_content/append_images(JSON)/append_at + إشعار داخلي للفني type='review_append' |
| تقديم التقييم | تسجيل POST /api/order/review/{order_id} (ReviewController::store لم يكن له مسار سابقًا)؛ مع إصلاح TypeError كامن: findByOrderId يستقبل int مخالفًا لتوقيع string (مقارنة بتحويل (string) في append)، التسجيل كشف الاستدعاء 500 |
| البيانات | appointment_order_review يضيف ثلاثة أعمدة append_content TEXT/append_images JSON/append_at DATETIME (ترحيل ذرّي)؛ الاستجابة تمرر حقول append |

### 34. تتبع الشحن في واجهة المستخدم (الجولة 19)

| الميزة | الوصف |
|------|------|
| الواجهة | GET /api/order/logistics/{id}: طلبات المنتجات الخاصة بالمستخدم فقط (غير المالك/غير المنتج/غير المشحون 404 موحد) |
| البيانات | قراءة order.remark JSON (shipping_company/tracking_no/shipped_at، تكتب عند الشحن من MallOrderController::ship في admin)؛ parseShippingInfo/parseReceiver تحليلان احتياطيان للصيغ القديمة |
| الإخفاء | رقم هاتف المستلم maskPhone (138****5678)، ضد التسريب |

### 35. إعدادات تفضيل الإشعارات (الجولة 19)

| الميزة | الوصف |
|------|------|
| البيانات | جدول appointment_user_notify_setting (مفتاح مركب فريد user_id+type uk_user_type، صف افتراضي = مفتوح)؛ 5 أنواع: service_reminder تذكير الخدمة / card_expiry تذكير الانتهاء (بطاقة + كوبون موحد بمظلة واحدة) / points_expiry انتهاء النقاط / marketing تسويقي (محجوز) / system نظام (لا يُغلق، PUT يفرضه 1) |
| الواجهة | GET /api/user/notify-settings يعيد 5 مفاتيح كاملة؛ PUT upsert جماعي دون صفوف مكررة |
| البوابة | NotificationReminderService::notifySettingEnabled مرتبط بـ 3 عمليات مؤقتة (ServiceReminderTimer/ExpiryReminderTimer بطاقة + كوبون/PointsExpiryTimer، المؤقتات تُدرج مباشرة في جدول appointment_notification دون مسار كتابة الخدمة لذا أُضيفت نفس البوابة لكل منها) + أحداث الاشتراك (sendSubscribeForOrderEvent/تعيين سيناريوهات Notification PAY/REFUND/VERIFIED/RESCHEDULE←system دائم الإرسال، REMINDER←service_reminder، EXPIRY←card_expiry)؛ عند إغلاق النوع تُتخطى الإشعارات الداخلية ورسائل الاشتراك معًا |

---

## ثانيًا: لوحة الإدارة (PC Web)

تطبيق Flutter Web بصفحة واحدة، إجمالي 21 صفحة: لوحة القيادة/المستخدمون/الأدوار/الإعدادات/السجلات/التحقق/الجدولة/الخدمات/الفنيون/الطلبات/الكوبونات/العضويات/بطاقات المرات/الإعلانات/الأسئلة الشائعة/السحب/التقييمات/التقارير/الملف الشخصي/لوحة عمل المتجر.

### 1. لوحة القيادة الرئيسية

- إحصاءات فورية: عدد المستخدمين/إجمالي الطلبات/عدد الفنيين/عدد طلبات الخدمة
- رسم بياني خطي: اتجاه الطلبات/اتجاه المبالغ/المستخدمون الجدد/النشاط
- تنقل سريع: أزرار الوحدات المعلقة
- رسائل داخلية: إشعارات الطلبات الجديدة/إشعارات الاسترداد

### 2. إدارة الفنيين

- قائمة الفنيين: بحث بـ UID/رقم الهاتف/الاسم/المنطقة/وقت التسجيل
- عرض القائمة: الرقم/UID/رقم الهاتف/الاسم المستعار/المُحيل/الحالة/عدد المتدربين/الأداء/حالة الحساب/وقت التسجيل/آخر تسجيل دخول/المنطقة
- العمليات: تصدير/تعديل المشرف/عرض المرؤوسين/تعديل كلمة المرور ورقم الهاتف/إدارة الجدولة/إعداد خدمات الفني/عرض تقدم الدورة
- الإضافة: الاسم/الجنس/رقم الهاتف/بطاقة الهوية/صورة بطاقة الهوية
- مراجعة طلبات الانضمام

### 3. إدارة المستخدمين

- قائمة الأعضاء: الاسم/رقم الهاتف/الصورة/المستوى/مبلغ الاستهلاك
- البحث: UID/رقم الهاتف/الاسم المستعار/وقت التسجيل
- العمليات: تفاصيل/تعديل المشرف/عرض المرؤوسين/تعديل كلمة المرور ورقم الهاتف/تعيين مستوى العضوية

### 4. إدارة المتاجر

- قائمة المتاجر: تفعيل/تعطيل/حذف
- إضافة متجر: الاسم/العنوان/الإحداثيات/الهاتف/ساعات العمل/الصورة

### 5. إدارة الخدمات

- قائمة الخدمات: بحث بالاسم/التصنيف؛ الرقم/الاسم/النوع/الخصم/أقل سعر/المبيعات/الغلاف/الترتيب/الحالة/الوقت
- العمليات: إضافة/تعديل/حذف/تصميم البطاقات
- قائمة المنتجات: النوع/الاسم/الخصم/أقل سعر/المبيعات/المخزون/الغلاف/الترتيب/الحالة/الوقت

### 6. إدارة المتجر الإلكتروني

- طلبات المتجر: تفاصيل/شحن/شحن/طباعة
- طلبات ما بعد البيع: عرض/مراجعة/طباعة
- إدارة التقييمات: عرض/مراجعة (show/hide)/حذف (ReviewController index/show/audit/destroy)
- سجل الدفع
- إحصاءات المبيعات

### 7. إدارة الطلبات

- طلبات بانتظار الاستخدام: بحث متعدد الشروط
- العمليات: تفاصيل/إلغاء من المنصة/تأكيد الإتمام

### 8. أنشطة الكوبونات

- القائمة: الرقم/الصورة/النوع/الاسم/الرفع والإنزال/الإجمالي/المتبقي/المدير/الوقت/تاريخ الانتهاء
- العمليات: إضافة/تعديل/حذف

### 9. الإدارة المالية

- تقسيم الطلبات: بحث/تفاصيل
- سحب الفنيين: مراجعة WithdrawalController؛ مبالغ ≥500 بموافقة مستويين (مدير المتجر store_approved_at ← المالية finance_approved_at)؛ آلة الحالة pending→approved→completed (rejected/failed)
- إعداد العمولة: تعديل نسبة العمولة/دورة التسوية/المكافآت والغرامات/الرصيد
- سجل الإيرادات والمصروفات
- إدارة حسابات السحب
- إعدادات قيود السحب

### 10. إدارة المحتوى

- CRUD الشرائح الدوارة
- إعدادات عنا
- مراجعة منشورات اللحظات
- CRUD الأسئلة الشائعة
- معالجة الملاحظات
- CRUD إعلانات المنصة

### 11. الإعدادات

- تحرير اتفاقيات المنصة (اتفاقية المستخدم/الخصوصية/الخدمة)
- إعداد عمولة الفنيين الموحدة
- قوالب رسائل النظام (تشمل إعداد قوالب رسائل اشتراك البرنامج الصغير، تنازل تلقائي عند عدم الإعداد)
- إدارة صلاحيات الحسابات الفرعية (مدير المتجر يمكنه إصدار كوبونات + جدولة)

### 12. الميزات الموسعة

- تصميم البطاقات: خدمة + منتج مركب/أجور يدوية/إعداد العمولة
- مراقبة النظام: لوحة حية لوحدة المعالجة/الذاكرة/القرص/Redis/MySQL/قوائم الانتظار
- القائمة السوداء IP: عرض هجمات security-php + حظر يدوي
- نسخ قاعدة البيانات: نسخ/تنزيل/استعادة عبر واجهة الويب
- صورة العميل: عرض 360/تفضيلات الاستهلاك/تسويق طبقي
- إرسال جماعي: رسائل قالبية/إرسال مجزأ
- تدفق مراجعة الاسترداد: موافقة بمستويين (مدير المتجر ← المالية)
- مستوى الفني: تقييم تلقائي junior/senior/expert
- المهام المجدولة: إلغاء تلقائي/تسوية/معالجة الانتهاء
- إعداد الرسائل النصية: إدارة قنوات Alibaba Cloud/Tencent Cloud المتعددة
- إعداد التخزين: محلي/OSS/COS/CDN
- تقارير محسّنة: حقول مخصصة/تقارير بريد مجدولة
- تصدير الجدولة: تصدير Excel لسجلات الحجز/قوائم الحضور
- قيد جنس الفني: تحكم الجنس لمشاريع محددة
- تدريب الفنيين: إدارة الدورات/تتبع تقدم التعلم
- حساب مدير المتجر: عزل بيانات store_id + صلاحيات مخصصة

### 13. تقارير البيانات (الجولة 7)

- ReportController 3 نقاط نهاية: إحصاءات الطلبات / أداء الفنيين / توزيع المتاجر
- تخزين Redis مؤقت svc:admin_report:{type}:{start}:{end}، TTL 300

### 14. إدارة بطاقات العضوية (الجولة 10)

- عمود مستوى العضوية appointment_user.member_level (ترحيل 000008)
- MemberCardController CRUD كامل (صلاحيات 365-369): GET/POST/PUT/DELETE /admin/member-cards
- صفحة إدارة تعريفات بطاقات العضوية Flutter

### 15. إدارة ما بعد البيع (الجولة 14)

- جدول appointment_order_aftersale (ترحيل 000009): type=refund/exchange، status=pending/approved/rejected/completed
- AftersaleController: GET /admin/aftersales (ترقيم + تصفية status/uid/order_no) + POST /admin/aftersales/{id}/review (approve/reject+remark)
- صفحة إدارة ما بعد البيع Flutter (قائمة + حوار مراجعة، صلاحيات 370/371)، التخطيط مسجل

### 16. لوحة عمل مدير المتجر (الجولة 15)

- service /api/store-manager: overview (طلبات اليوم/الإيرادات/الجاري/عدد الفنيين/عدد التحقق) + orders (ترقيم + تصفية حالة) + technicians (مع جدولة اليوم) + revenue (تجمع آخر 7 أيام)، requireStoreId() يفرض عزل store_id (بدون متجر 403)
- admin StoreController::workbenchOverview (GET /admin/stores/workbench-overview?store_id=، معيار متسق مع service) + قائمة طلبات AppointmentOrderController تصفية store_id (فك ترميز hashid)
- صفحة لوحة عمل المتجر Flutter: قائمة متاجر منسدلة + تصفية حالة + 5 بطاقات نظرة عامة + جدول طلبات DataTable + ترقيم (صلاحية 372)

### 17. منتجات صرف النقاط (الجولة 16)

- PointsExchangeGoodsController: GET/POST/PUT/DELETE /admin/points-exchange-goods + POST {id}/toggle-status (رفع/إنزال) + GET {id}/exchanges (سجلات الصرف، مع رقم الهاتف + تحليل result JSON)
- ترحيلان 000012 (جدولان) + 000013 (صلاحيات 373-378) مطبقان

### 18. سجلات العمولة (الجولة 16)

- ReferralRewardController: GET /admin/referral-rewards (سجلات rewarded_at غير فارغة فقط، ترقيم + تصفية keyword باسم/هاتف المُحيل أو المُحال، ترميز hashid، صلاحية 379)

### 19. تقييم تلقائي لمستوى الفني (الجولة 17)

- TierRatingService::evaluate(technicianId, allowDowngrade=false): إحصاء فوري لعدد طلبات appointment_order completed + متوسط درجات appointment_order_review (تقريب لخانة عشرية واحدة) للكتابة في profile.order_count/rating، مطابقة من الأعلى للأسفل وفق appointment_technician_tier_config (min_orders/min_rating)، بلا تطابق يُحسب أدنى مستوى
- قواعد الترقي/التنزيل: ترقية فقط دون تنزيل (المستوى مربوط بنسبة العمولة ومعامل السعر، التنزيل التلقائي يؤثر على دخل الفني ويثير النزاعات، التراجع يُدار يدويًا في admin)؛ allowDowngrade=true (سيناريو إعادة التقييم اليدوي في الخلفية) فقط ينفذ التنزيل، والتنزيل يسجل في السجلات + الإشعارات
- الذرّية: عند تطابق المستوى المستحق مع profile.tier_id يكتفي بمزامنة الإحصاء دون كتابة سجل أو إرسال إشعار
- السجلات: التغيير يكتب appointment_technician_tier_log (id/technician_id/old_tier_id/new_tier_id/reason/created_at) + إشعار داخلي (type='tier')
- نقاط التشغيل: WorkController::complete / كتابة تقييم ReviewController / ProfileController فحص كسول عند عرض الملف
- لوحة الإدارة: TechnicianTierController يحافظ على إمكانية التكوين اليدوي؛ GET /admin/technician-tiers/logs ترقيم لعرض سجلات التغيير (join اسم الفني واسمي المستويين، ID بترميز hashid، صلاحية 380)

### 20. عرض ردود التقييم (الجولة 18)

- ReviewController يضيف reply(): GET /admin/reviews/{id}/reply تفاصيل الرد (decodeId ← find ← 404 ← إخراج decorate، عند عدم الرد reply=''، reply/replied_at تمرران عبر toArray)
- المسار مسار ثابت (قبل audit، معرَّف قبل resource)؛ بذرة صلاحية id 381 (slug 'get.admin/reviews/{id}/reply'، type 3، ربط ذرّي بدور المشرف)
- نقطة الصلاحية: 381

### 21. تقويم الحجوزات (الجولة 20)

- CalendarController عرض شهري/يومي: GET /api/calendar/technician/{id} (شهري) + /day (يومي)
- مصدر البيانات: time_slots JSON في technician_schedule يُفتح إلى فتحات ساعات حسب أيام الأسبوع، استبعاد فترات appointment_order المحجوزة ذلك اليوم (status ∈ pending/paid/confirmed/serving)، إخراج الفتحات المتبقية
- الاستخدام: اختيار مرئي لوقت جدولة المتجر، الواجهة تمرر أفقيًا يوميًا + اختيار خلايا الوقت

### 22. مستوى نمو المستخدم (الجولة 20)

- appointment_user_growth (سجلات) + appointment_growth_level (بذور 5 مستويات: برونزي 0/فضي 100/ذهبي 500/بلاتيني 2000/ماسي 5000)
- نقاط إيداع النمو: الحضور +10 (CheckInController)؛ تقديم التقييم +20 (ReviewController::store، الإضافة لا تُودع)؛ الاستهلاك floor(paid) كل 1 يوان نقطة واحدة (WechatPayService::markOrderPaid، إعادة استخدام التحقق من حالة الدفع القائمة كذرّية طبيعية، الاستدعاء المكرر لا يُودع مرتين)
- الواجهات: GET /api/growth (نظرة المستوى الحالي: balance/level/فرق المستوى التالي)؛ GET /api/growth/records (سجل مرقّم)؛ GET /api/growth/levels (قائمة مستويات عامة، بلا تسجيل دخول)
- إستراتيجية الفشل: أي نقطة إيداع try/catch تسجل في السجلات دون التأثير على العملية الرئيسية

### 23. الفاتورة الإلكترونية (الجولة 20)

- appointment_invoice: uk_order_type(order_id,order_type) ضد طلب مكرر لنفس الطلب (طلب مكرر 422، مع التقاط MySQL 1062 احتياطيًا)؛ idx_user_created/idx_status
- واجهة المستخدم: POST /api/invoices (طلب، المبلغ/الترويسة تُؤخذ من الخادم من الطلب، لا يمكن التلاعب)؛ GET /api/invoices (قائمة)؛ GET /api/invoices/{id} (تفاصيل)
- لوحة الإدارة: InvoiceController issue (إصدار: كتابة invoice_no + status=issued + issued_at) / reject (رفض: status=rejected + reject_reason)، صلاحيات 382 قائمة/383 إصدار/384 رفض
- آلة الحالة: pending ← issued / rejected

### 24. تذاكر خدمة العملاء (الجولة 20)

- appointment_ticket: تقديم المستخدم للخوذة (title/content)، رد الخلفية يُلحق (reply_content/replied_at)، المستخدم يمكنه الإغلاق (closed_at)
- واجهة المستخدم: POST /api/tickets (تقديم)؛ GET /api/tickets (قائمة)؛ GET /api/tickets/{id} (تفاصيل، المالك فقط)؛ POST /api/tickets/{id}/close (إغلاق)
- لوحة الإدارة: TicketController index (قائمة) / reply (رد)، مسارات ثابتة قبل resource لتجنب تظليل {id}؛ صلاحيات 385 رد التذاكر/387 عرض قائمة التذاكر
- آلة الحالة: open ← replied (بعد الرد يعود open ويمكن الرد مجددًا) / closed

### 25. توزيع متعدد المستويات — عمولة المستوى الثاني (الجولة 20)

- ReferralRewardService::payLevel2Reward(paidAmount, orderId): بعد نجاح دفع الطلب، يُبحث مُحيل المُحيل المباشر (علاقة إحالة المستوى الثاني)، إصدار paid×level2_rate (إعداد النظام referral.level2_rate، افتراضي 0.02)
- الذرّية: قفل صفوف داخل معاملة + مفتاح فريد uk_order_referred(order_id, level2_user_id)، استدعاءات الدفع المكررة/التزامن لا يصدرون مرتين؛ try/catch فشل يسجل فقط دون التأثير على دفع الطلب الرئيسي
- الإيداع: WalletTxn type='referral_level2' (ثابت TYPE_REFERRAL_LEVEL2) + تراكم رصيد المحفظة
- لوحة الإدارة: ReferralLevel2Controller index سجلات مرقمة (صلاحية 386)، join اسمي مستخدمي المستويين

### 26. إطلاق مزايا مستوى النمو (الجولة 21)

- إطلاق نموذج GrowthLevel.benefits JSON: بذور ترحيل 5 مستويات (برونزي {"discount_rate":1.0,"points_multiplier":1.0}، فضي 0.98/1.1، ذهبي 0.95/1.2، بلاتيني 0.92/1.3، ماسي 0.9/1.5)
- خصم المستوى: OrderController::store applyGrowthDiscount() — الطلبات القياسية فقط (promotion_id فارغ، الشراء الجماعي/الفلاش تعطيل التراكب)؛ الترتيب: بعد خصم الكوبون/بطاقة المرات، المبلغ المستحق × discount_rate؛ مبلغ الخصم يندمج في discount_amount، ملاحظات الطلب تُلحق «خصم المستوى: فضي 9.8، وفورات ¥2.00» قابلة للتتبع؛ حماية أدنى سعر: الدفع بعد الخصم ≥ 0.01 يوان (نقاط ≥ 100)، دون ذلك يُقتطع الخصم إلى 0
- مضاعف النقاط: WechatPayService::markOrderPaid قيمة النمو من floor(paid) إلى floor(paid × points_multiplier)، المضاعف يُلتقط بمستوى وقت الدفع (تراكم قبل الإيداع، هذا الطلب لا يرفع المستوى)؛ نقطة ربط try/catch في R20 محفوظة كاملة
- إعادة استخدام الاستعلام: GrowthLevel::levelForGrowth() يلتقط المستوى بالنمو التراكمي، للاستخدام في الطلب/الدفع؛ GET /api/growth يعيد benefits وnext_gap (تنفيذ R20، دون تغيير)

### 27. إدارة ترويسة الفاتورة (الجولة 21)

- appointment_invoice_title (uk_user_title(user_id, title_type, invoice_title) ضد التكرار + idx_user_default)
- الواجهات: POST /api/invoice-titles (حفظ، company يلزم tax_no، مكرر 422)؛ GET (قائمة، الافتراضي أعلى)؛ PUT /{id} (تعديل، المالك فقط)؛ DELETE /{id} (حذف، المالك فقط)؛ POST /{id}/default (تعيين افتراضي، تصفير معاملة لصفوف المستخدم الأخرى)
- قاعدة الافتراضي: أول ترويسة محفوظة تلقائيًا افتراضية؛ حذف الافتراضي يعين تلقائيًا الأقدم
- ربط الطلب: InvoiceController::store title_id اختياري يحلل الترويسة لجلب invoice_title/tax_no/title_type، بدون title_id يحتفظ بمسار الإدخال اليدوي الأصلي؛ منطق uk_order_type المضاد للتكرار دون تغيير

### 28. رضا التذاكر (الجولة 21)

- appointment_ticket يضيف rating TINYINT NULL + rated_at DATETIME NULL (ترحيل 000303)
- تقييم الإغلاق: TicketController::close() يدعم rating اختياري 1-5 (تحقق أعداد صحيحة filter_var، خارج النطاق/غير صحيح 422؛ عند تقديمه يكتب rating+rated_at، عند عدم تقديمه يبقى NULL توافقًا مع العملاء القدامى؛ قاعدة إغلاق التذاكر المفتوحة فقط محفوظة)
- إحصاءات الخلفية: GET /admin/tickets/satisfaction (مسار ثابت قبل resource لتجنب تظليل {id}) يعيد total/rated_count/unrated_count/average (خانة عشرية واحدة)/distribution (أعداد 1-5 نجوم، النجوم الناقصة تُكمل 0)؛ صلاحية 388

### 29. مراجعة صور التقييمات (الجولة 21)

- admin ReviewAuditController (جديد، دون المساس بـ ReviewController الحالي): GET /admin/review-audit قائمة التقييمات بالصور (تصفية JSON_LENGTH(images)>0 + leftJoin اسم المستخدم واسم الفني + تصفية status + ترميز hashid)؛ POST /{id}/hide إخفاء؛ POST /{id}/restore استعادة
- آلة الحالة: hide فقط visible يُخفى، restore فقط hidden يُستعاد (422 ثنائي الاتجاه)؛ حالة OrderReview نظام أعداد صحيحة (STATUS_HIDDEN=0/STATUS_VISIBLE=1)
- سلسلة التأثير: قائمة تقييمات الفني في واجهة المستخدم مصفاة بالفعل حسب status ← بعد الإخفاء تختفي تلقائيًا
- الصلاحيات: 389 قائمة / 390 إخفاء / 391 استعادة

### 30. سجل تصفح المستخدم (الجولة 21)

- appointment_browse_history (uk_user_item(user_id, item_id) فريد، التصفح المكرر يحدّث viewed_at فقط دون إدراج مكرر؛ idx_user_viewed للترتيب)
- ربط السجل: ServiceController::detail() يسجل بعد النجاح (try/catch + Log::warning دون تأثير على العملية الرئيسية؛ المسارات العامة بلا JWT، user_id فارغ يتخطى المجهولين)
- الواجهات: GET /api/browse-history (join appointment_service الاسم/الغلاف/السعر/السعر الأصلي، viewed_at تنازلي، per_page افتراضي 15 سقف 50، item_id hashid)؛ DELETE /{item_id} (المالك فقط، غير صالح/لشخص آخر 404)؛ DELETE / (مسح للمالك فقط)

### 31. تسويق التخفيض (الجولة 22)

- appointment_full_reduction_activity (threshold/reduction/title/status/start_at/end_at + idx_status_status_time)
- تراكب الطلب: الطلبات القياسية فقط (الشراء الجماعي/الفلاش تُتخطى)، الحكم بالحد على المبلغ المستحق بعد خصم الكوبون/بطاقة المرات، الترتيب **كوبون/بطاقة مرات ← تخفيض ← خصم مستوى**؛ اختيار النشاط بأكبر مبلغ خصم؛ مبلغ الخصم يندمج في discount_amount + ملاحظات «تخفيض: فوق X خصم Y»؛ بعد التخفيض حد أدنى للدفع 0.01 يوان (نقاط)
- واجهة المستخدم GET /api/full-reduction-activities (عام، السارية بترتيب تنازلي لمبلغ الخصم)
- admin FullReductionController: CRUD + toggle-status رفع/إنزال (destroy مع confirmPassword)
- الصلاحيات: 396 قائمة / 397 إضافة / 398 تعديل / 399 رفع وإنزال / 400 حذف (سجل صلاحية واحد يقابل method.path slug واحد، 5 مسارات تقسم 5 سجلات)

### 32. تصدير حجوزاتي ICS (الجولة 22)

- IcsController GET /api/order/ics: طلبات 90 يومًا pending/paid/confirmed/serving تصدير iCal (RFC5545)، المالك فقط
- VEVENT: UID=معرّف الطلب، DTSTAMP(UTC)، TZID=Asia/Shanghai، مدة افتراضية ساعة، ملخص «حجز: اسم الخدمة» (تراجع «حجز» عند الغياب)، وصف الفني/المتجر/العنوان (تخطي عند الغياب)، LOCATION؛ هروب النصوص (\\, \; \\ \n) + طي سطور 75 بايت
- بدون طلبات يعيد تقويمًا فارغًا صالحًا (هيكل `BEGIN:VCALENDAR`)

### 33. حضور الفنيين (الجولة 22)

- appointment_technician_attendance (date/check_in_at/check_out_at/status + فهرس فريد uk_technician_date ضد الحضور المكرر المتزامن)
- واجهة الفني (TechnicianAuth): check-in تكرار اليوم 422؛ check-out لم يبدأ العمل/غادر بالفعل 422 + قفل صفوف؛ >10:00 يُعلَّم تأخرًا؛ GET قائمة الشهر + عدد أيام الحضور/إجمالي الساعات/متوسط الساعات (?month=YYYY-MM غير صالح 422)
- admin: GET /admin/attendance (تصفية date+اسم الفني، join real_name، hashid) + /stats (إحصاءات مجمعة حسب الفني)
- الصلاحيات: 392 قائمة / 393 إحصاءات

### 34. خدمة دفع APP (الجولة 22)

- AppPushService (config group=push: enabled افتراضي 0 / provider jpush/getui/placeholder): غير مفعل تنازل صامت إلى سجلات فقط؛ عند التفعيل يبني platform/title/content/payload يسجل في Log + يكتب appointment_push_log (status=sent)؛ ربط SDKات الموردين متروك TODO (بلا بيانات اعتماد لا إرسال فعلي)
- ربط 5 أحداث: نجاح الدفع (WechatPayService::markOrderPaid)، الاسترداد التلقائي (autoRefundCancelledOrder)، الاسترداد اليدوي (doRefund/refundToBalance)، تعويض الاسترداد (completeOneRefundCompensation)، تذكير بدء الخدمة (ServiceReminderTimer)؛ جميعها try/catch لا تعيق العملية الرئيسية
- appointment_push_log (user_id/title/content/payload JSON/status/provider + idx_user)

### 35. تقسيم الأرباح الرسمي من WeChat (الجولة 22)

- WechatProfitSharingService (config group=profit_sharing: enabled/receiver_ratio، بيانات الاعتماد تعيد استخدام wechat_pay): غير مفعل disabled تنازل إلى سجلات فقط دون كتابة؛ عند التفعيل ← تحقق المبلغ (>0 و≤paid، الدفع الفعلي × 0.7 افتراضي) + ذرّية (نفس الطلب pending/success يُتخطى) ← كتابة سجل pending ← بناء «طلب تقسيم لمرة واحدة» (بلا بيانات اعتماد لا تنفيذ HTTP، محتوى الطلب يسجل، السجل يبقى pending)؛ HTTP معزول doRequest خاص قابل للاختبار
- WechatPayService::markOrderPaid بعد التقديم يرتبط بـ requestSharing (try/catch فشل يسجل فقط)
- appointment_profit_sharing (uk_sharing_no فريد + idx_order)؛ admin GET /admin/profit-sharing قائمة (join رقم الطلب/اسم الفني، تصفية حالة/رقم طلب/اسم فني)
- الصلاحية: 394

### 36. خصوصية الامتثال (الجولة 22)

- GET /api/privacy/data: تصدير البيانات (personal/orders/points/wallet_txns/reviews/addresses/invoices مجمعة؛ السجلات تسجل رقم الهاتف المموه + العدد فقط)
- إغلاق الحذف: close-request (رصيد غير صفري / طلبات غير مكتملة / تذاكر جارية 422 ← close_status=1) ← close-cancel (1←0) ← close-confirm (إتمام 72 ساعة ← close_status=2 + close_at + إخفاء هوية phone/nickname إلى user{id} + status=0)
- appointment_user يضيف close_status/close_requested_at/close_at (ترحيل ALTER ذرّي)؛ AuthController login/loginByCode لـ close_status=2 يعيدان 403 «الحساب محذوف»

### 37. الملف الصحي للمستخدم (الجولة 23)

- GET/PUT/DELETE /api/health-profile: ملف واحد لكل شخص (فهرس فريد uk_user)، upsert يحدّث الحقول المقدمة فقط
- allergies/health_notes سقف 500 حرف، preferred_technician_id يتحقق من الوجود، الاستجابة بترميز hashid
- ترحيل 000504_user_health_profile؛ HealthProfileTest 6 اختبارات

### 38. كلمة مرور دفع المحفظة (الجولة 23)

- POST /api/wallet/pay-password/{set,verify,check}: تحقق 6 أرقام، تخزين password_hash + pay_password_set_at
- عند الضبط، التعديل يتطلب كلمة المرور القديمة 422؛ verify تحقق فقط دون كتابة؛ check يعيد هل أُعدّت
- ترحيل 000502 (ALTER ذرّي INFORMATION_SCHEMA لعمودين)؛ WalletPayPasswordTest 7 اختبارات

### 39. جدولة جماعية للفنيين (الجولة 23)

- POST /api/technician/schedule/batch: نطاق أيام ≤ 7 + تصفية weekdays، أيام الجدولة الموجودة تُتخطى
- الإعداد الفردي يفعل أيضًا فحص تداخل الفترات (422 «تعارض مع جدولة قائمة: HH:MM-HH:MM»)
- ScheduleConflictTest 5 اختبارات

### 40. خط زمني لحالة الطلب (الجولة 23)

- GET /api/order/{id}/timeline: المالك فقط (الآخرون 404)، إرجاع تنازلي؛ تفاصيل طلب admin تدمج مصفوفة timeline
- OrderStatusLog::record() نقاط ثابتة لـ 8 أنواع تغيير: تقديم/دفع/إلغاء/تأكيد/طلب استرداد/تمرير استرداد/بدء خدمة/إتمام خدمة/إلغاء تلقائي بمهلة/عملية خلفية (operator=admin)
- استدعاء الدفع markOrderPaid نقطة استهلاك وحيدة؛ record() داخليًا try/catch + Log::warning لا يعيق العملية الرئيسية أبدًا
- ترحيل 000501_order_status_log؛ OrderTimelineTest 4 اختبارات

### 41. عجلة حظ النقاط (الجولة 23)

- GET /api/wheel/prizes (إخفاء weight/stock)؛ POST /api/wheel/spin: Redis NX + قفل صفوف ضد التزامن، سحب موزون random_int، client_token ذرّي
- إيداع الجوائز: نقاط ← سجل earn (مع وقت انتهاء، يمكن لـ PointsExpiryTimer انتهاؤه طبيعيًا)، رصيد ← lockForUpdate، كوبون ← pending إصدار يدوي، بلا جائزة ← lose
- GET /api/wheel/records سجلاتي مرقمة؛ admin /admin/lucky-wheel CRUD + رفع/إنزال + سجلات (صلاحيات 401-406)
- ترحيلان 000503 (appointment_lucky_wheel + appointment_wheel_record + بذور تجريبية w60/w40) + 000505 (بذور صلاحيات)؛ LuckyWheelTest admin 3 + service 6 اختبارات

### 42. وضع الضيف (الجولة 24)

- GET /api/guest/{home,services,services/{id},stores,technicians}: مدخل تصفح بدون تسجيل دخول دون مصادقة (وسيط ApiVersion فقط)
- home يجمع الشرائح الدوارة/الإعلانات/تصنيفات الخدمات/الخدمات الشائعة، تخزين Redis مؤقت svc:guest:home 300 ثانية؛ services يدعم تصفية التصنيف + ترتيب newest/sales/price (page/per_page≤50)؛ technicians فقط المعتمدون، مع تصفية service_id، بترتيب تنازلي للدرجة
- تغطية GuestControllerTest

### 43. الفلاش (الجولة 24)

- appointment_seckill_activity (name/service_id/seckill_price/original_price/stock/start_at/end_at/status)؛ الكمية المباعة = عدد طلبات appointment_order.seckill_id
- GET /api/seckill (status=1 + نافذة زمنية)، /{id} (state=not_started/ongoing/ended)، POST /{id}/buy: client_token (8-64 حرفًا، SETNX 24 ساعة) ذرّي + Redis NX 30 ثانية ضد التزامن + تحقق النشاط (منذ 2026-08-26 لا حجز مسبق للمخزون)
- حقن seckill_id في الطلب يعيد استخدام OrderController::store؛ المخزون يُخصم موحدًا بقفل صفوف داخل معاملة store() (استدعاء /api/order مباشرة مع seckill_id يخصم المخزون أيضًا)، سعر الفلاش = seckill_price (يتبع قاعدة البيانات)، دون تراكب كوبون/نقاط/بطاقة عضوية؛ إلغاء الطلب لا يعيد المخزون؛ قناة الترويج FLASH_SALE القديمة حُذفت (فرع الترويج في store() بقي للشراء الجماعي فقط، PromotionController index يفلتر flash_sale وshow/join يعيدان 400)، الفلاش فقط عبر هذه القناة
- admin /admin/seckill CRUD + رفع/إنزال + قائمة الطلبات (صلاحيات 407-411 و420)؛ بذور صلاحيات ترحيل 000606؛ SeckillTest service + admin

### 44. إدارة إصدارات APP والتحقق من التحديث (الجولة 24)

- appointment_app_version (platform/version_code/version_name/force_update/changelog/download_url/status)
- GET /api/app/version?platform=android|ios فحص تحديث عام (platform غير صالح 422؛ الأحدث بين status=1؛ بلا كائن فارغ)
- admin /admin/versions CRUD (صلاحيات 416-419)؛ بذور صلاحيات ترحيل 000609؛ VersionTest service + admin

### 45. مكافأة العميل العائد (الجولة 24)

- ReturnCustomerRewardService: استهلاك المستخدم الثاني لنفس الفني خلال 30 يومًا (إتمام الطلب) يمنح الفني مكافأة = الدفع الفعلي paid_amount × ratio (system_config group=return_customer، ratio افتراضي 0.05 وenabled مفتاح، القيم غير الصالحة ترجع للافتراضي)
- كتابة appointment_technician_earnings (type=return_customer، status=pending) يعيد استخدام سلسلة تسوية العمولة، ملخص earnings لواجهة الفني يتضمنه تلقائيًا؛ نفس order_id+type ذرّي؛ استدعاء داخل معاملة قفل صفوف WorkController::complete
- admin /admin/return-customer/config (GET/PUT) + /rewards (?keyword اسم فني/رقم طلب/اسم مستعار) (صلاحيات 412-414)؛ بذور صلاحيات ترحيل 000607؛ ReturnCustomerRewardServiceTest

### 46. تصدير الجدولة (الجولة 24)

- GET /admin/technician-schedule/export: CSV (UTF-8 BOM، يُفتح مباشرة في Excel)، اسم الملف schedules_{YmdHis}.csv
- start_date/end_date إلزاميان (YYYY-MM-DD، غير صالح 422) ونطاق ≤ 31 يومًا؛ technician_id اختياري (hashid، غير صالح 422)
- الأعمدة: معرّف الفني/اسم الفني/التاريخ/تفاصيل الفترات (تحليل time_slots JSON إلى "09:00-12:00, 14:00-18:00")
- الصلاحية: 415؛ بذور صلاحيات ترحيل 000608؛ تغطية ScheduleExportTest
