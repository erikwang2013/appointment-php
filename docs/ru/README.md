> Русский перевод · Оригинал: [中文](../../README.md)

# Система сервиса предварительной записи
> **Languages**: [中文](../README.md) · [English](../en/README.md) · [한국어](../ko/README.md) · [Deutsch](../de/README.md) · [Français](../fr/README.md) · [Español](../es/README.md) · [Português](../pt/README.md) · [हिन्दी](../hi/README.md) · [العربية](../ar/README.md) · [বাংলা](../bn/README.md) · [Bahasa Indonesia](../id/README.md) · [日本語](../ja/README.md)

Платформа управления предварительной записью на четырёх платформах: WeChat Mini Program + Flutter APP + HarmonyOS APP для пользователей (переключение ролей под одной учётной записью) + PC-админка.

> **Статус проекта**: всё выполнено ✅ | 143 контроллера (service 69 / admin 74) | 87 моделей | 722 теста (service 558 / admin 164) | 95 таблиц БД | 388 маршрутов (service 227 / admin 161)

## О проекте

<img src="../diagrams/mascot.svg" alt="Талисман системы — кролик записи (SVG-анимация)" width="200" align="right">

**Система сервиса предварительной записи** — это платформа управления записью на четырёх платформах для сферы бытовых услуг: пользовательская часть охватывает **WeChat Mini Program, Flutter APP, HarmonyOS APP** — три платформы с свободным переключением под одной учётной записью, в паре с **PC-админкой** реализуется цифровой замкнутый контур всего процесса «пользователь записывается → мастер принимает заказ → операционная работа в бэкенде». Записи в салон, услуги мастеров, маркетинг для членов или финансовые расчёты — всё решает одна система.

**Удобство записи «в одно окно»**

Пользователи на трёх платформах получают одинаковый опыт: наглядный выбор времени по календарю, скидки по купонам/картам посещений/баллам, акции «молниеносная распродажа» и групповые покупки, оплата WeChat/балансом, статус заказа отслеживается на всём пути — перенос, отмена, возврат, послепродажное обслуживание и электронные счета оформляются онлайн; для мастеров предусмотрены рабочее место, отметка начала/конца рабочего дня, массовое составление расписания, подтверждение услуг и одобрение вывода средств — эффективность работы наглядна.

**Сквозной маркетинговый рост**

Встроено более десяти маркетинговых инструментов: акции «скидка при достижении суммы», «молниеносная распродажа», групповые покупки, передача купонов, бонусный магазин и колесо удачи, карты участника/уровни роста с привилегиями, двухуровневая дистрибьюторская комиссия, награды постоянным клиентам и т.д. В сочетании с подпиской на push-сообщения и APP-пушем система помогает бизнесу привлекать новых клиентов, удерживать и стимулировать повторные покупки.

**Безопасность и соответствие корпоративного уровня**

Используются собственные компоненты безопасности: JWT-аутентификация, обфускация ID, обнаружение 31 типа атак, двухслойное шифрование чувствительных данных, серверная проверка цен, строгое сравнение и идемпотентная защита от повторов в платежных колбэках; также поддерживаются официальное разделение средств WeChat, экспорт личных данных и удаление учётной записи — соответствие требованиям законодательства обеспечено.

**Зрелая технологическая база**

В основе PHP 8.3 + высокопроизводительный резидентный фреймворк webman, поддержка MySQL 8.0 + Redis + Elasticsearch; 95 таблиц БД, 388 интерфейсов, 285 детальных точек прав, все 722 автоматизированных теста проходят; есть полноценная китайско-английская архитектурная документация и скрипт установки в один клик — система готова к использованию и легко дорабатывается.

Будь то запись в один салон или сеть из нескольких филиалов, система сервиса предварительной записи предоставит вам стабильное, безопасное и масштабируемое комплексное решение.

## Структура проекта

```
appointment-php/
├── admin/                     # Админка (webman v2 + Flutter Web, отдельное развёртывание :8787)
│   ├── app/                   #   admin(контроллеры админки)/api/model/middleware/process/view
│   ├── apps/                  #   Flutter Web-админка / HarmonyOS / WeChat-управление
│   ├── config/                #   Конфигурация маршрутов/БД/процессов/плагинов
│   ├── database/              #   Скрипты резервного копирования (структура и сид-данные едины в docs/install.sql)
│   ├── tests/                 #   PHPUnit (стиль атрибутов #[\Test])
│   └── start.php
├── service/                   # Сервис бизнес-API (webman v2, отдельное развёртывание :8787)
│   ├── app/                   #   Модули api/user/technician/order/wallet/marketing/notification и др.
│   ├── config/                #   Конфигурация маршрутов/БД/процессов/платежей и др.
│   ├── support/               #   Базовый класс Model (generateId)/Request/Response
│   ├── tests/                 #   PHPUnit
│   └── start.php
├── apps/                      # Пользовательские фронтенд-приложения
│   ├── wechat/                #   WeChat Mini Program (нативная)
│   ├── flutter/               #   Flutter APP (iOS + Android)
│   └── harmonyos/             #   HarmonyOS APP (нативный)
└── docs/                      # Документация проекта
    ├── API.md / FEATURES.md / STRUCTURE.md / install.sql / README.md ...
    └── diagrams/              #   Архитектура/блок-схемы (SVG + mermaid)
```

## Быстрый старт

### Требования к окружению

- PHP 8.3+
- MySQL 8.0+
- Redis
- Composer

### Web-мастер установки (рекомендуется)

```bash
cd admin/
cp .env.example .env
composer install
php start.php start -d
```

Откройте в браузере `http://localhost:8787/install` и заполните данные БД и учётную запись администратора по подсказкам — установка завершена.

### Установка вручную

```bash
# 1. Установка зависимостей
cd service/ && cp .env.example .env && composer install
cd ../admin/ && cp .env.example .env && composer install

# 2. Импорт БД одной командой (все 95 таблиц + сид прав/конфигурации)
mysql -u root -p < docs/install.sql

# 3. Запуск сервисов
cd service/ && php start.php start -d   # Бизнес-API → :8787
cd ../admin/ && php start.php start -d  # Админка → :8787
```

### Развёртывание через Docker

```bash
cd admin/ && cp .env.docker .env && docker-compose up -d
cd ../service/ && cp .env.docker .env && docker-compose up -d
```

## Технологический стек

| Уровень | Технология | Описание |
|------|------|------|
| Бэкенд-фреймворк | webman v2 (PHP 8.3+) | Высокопроизводительный резидентный HTTP-сервис |
| БД | MySQL 8.0 | Префикс таблиц `appointment_` |
| Кэш | Redis | Кэш/ограничение частоты/Session/очередь |
| Поиск | Elasticsearch | Полнотекстовый поиск (via webman-scout) |
| Фронтенд админки | Flutter Web | Стиль PC-админки |
| Пользовательский APP | Flutter | iOS + Android |
| Пользовательская мини-программа | Нативная WeChat Mini Program | WXML/WXSS/JS |
| Пользовательский HarmonyOS APP | HarmonyOS ArkTS | Нативный @ohos.net.http |
| Генерация ID | erikwang2013/snowflake-php | BIGINT-первичные ключи без автоинкремента |
| Шифрование/дешифровка ID API | erikwang2013/hashids | Скрытие реальных ID от внешнего мира |
| JWT-аутентификация | erikwang2013/jwt-webman | Bearer Token |
| Шифрование чувствительных данных | erikwang2013/encryption + encryptable | Двухслойное шифрование API + DB |
| Защита | erikwang2013/security-php | Обнаружение 31 типа атак |
| Проверка операций | erikwang2013/poster-php | Случайная проверка чувствительных операций |
| Флаги стран | erikwang2013/season | Иконки флагов |
| Синхронизация ES | erikwang2013/webman-scout | Автосинхронизация моделей |

## Архитектура системы

<img src="../diagrams/ru-architecture.svg" alt="ru-architecture.svg" width="100%">

## Ключевые процессы

### Процесс записи на услугу

<img src="../diagrams/ru-appointment-flow.svg" alt="ru-appointment-flow.svg" width="100%">

### Процесс оплаты и возврата

<img src="../diagrams/ru-payment-refund.svg" alt="ru-payment-refund.svg" width="100%">

## Жизненный цикл заказа

<img src="../diagrams/ru-order-lifecycle.svg" alt="ru-order-lifecycle.svg" width="100%">

## Архитектура безопасности

### Семиуровневая система эшелонированной обороны

<img src="../diagrams/ru-security-defense.svg" alt="ru-security-defense.svg" width="100%">

> Больше подробных схем: [Блок-схемы](diagrams/FLOWCHART.md) (включая вывод средств мастера/смену ролей) | [Функциональная карта](diagrams/FUNCTION-DIAGRAM.md) | [Все жизненные циклы](diagrams/LIFECYCLE-DIAGRAM.md) | [Полная архитектура безопасности](diagrams/SECURITY-ARCHITECTURE.md)

## Ключевые возможности (раунды 6-24)

| Возможность | Описание |
|------|------|
| Балансовый кошелёк | Таблицы user_wallet / wallet_recharge / wallet_txn; баланс + история операций, пополнение через WeChat Pay (номер с префиксом R в колбэке), оплата заказа балансом (pay_channel=balance), автоматическое возвращение на баланс при возврате WeChat/балансом |
| Полный UI админки | 21 страница Flutter Web: dashboard/пользователи/роли/конфигурация/логи/подтверждение/расписание/услуги/мастера/заказы/купоны/участники/карты посещений/объявления/FAQ/вывод средств/отзывы/отчёты/послепродажное обслуживание/рабочее место магазина/личный кабинет |
| Статистика в реальном времени | На главной админки 7 карточек статистики (всего пользователей/новые сегодня/активные/журнал операций/записи сегодня/ожидают вывода/ожидают проверки мастера) + графики за 30 дней (заказы/суммы/новые пользователи/активность) + круговая диаграмма статусов пользователей + последние журналы операций, кэш Redis svc:dashboard 300s |
| Отчёты данных | 3 конечные точки ReportController: статистика заказов / ТОП-10 мастеров / распределение каналов (GET /admin/reports/orders\|technicians\|distribution, диапазон 7/30 дней, кэш Redis 300s) + статистика продаж (svc:sales_stats) + финансовая статистика (svc:finance_stats доходы/возвраты/выводы/комиссии) |
| Подписка на сообщения мини-программы | Подписка по 3 сценариям заказа (оплата успешна/возврат поступил/подтверждение успешно); идемпотентность push_sent_at; при отсутствии шаблона автоматический переход на внутрисайтовые уведомления |
| Вывод средств мастера | Проверка в админке; сумма ≥500 — двухуровневое одобрение (управляющий→финансы); конечный автомат pending→approved→completed (rejected/failed) |
| Замкнутый цикл подтверждения карт посещений | Мои карты посещений — расчёт used_up/expired в реальном времени; подтверждение с идемпотентностью Redis NX + блокировка строк при списании, прямое создание заказа completed + OrderItem + OrderPayment(pay_type='card') |
| Рабочее место мастера | Задачи на сегодня/записи о выполнении/начать·завершить (блокировка строк + стражи конечного автомата + идемпотентность, после завершения — внутрисайтовое уведомление); три вкладки tech-work в мини-программе |
| Скидка по купонам | PriceCalculator: applyCoupon только считает сумму / consume при оплате ставит used / restoreCouponAndCard идемпотентно возвращает при возврате; фиксированная/процентная скидка + порог min_amount |
| Подарочная карта | При redeem тип cash пополняет кошелёк (блокировка строк против двойного зачисления, WalletTxn type='gift_card'), тип gift только помечается |
| Бонусная система | Бонусы за ежедневную отметку; бонусы за покупки при подтверждении floor(paid×1) (идемпотентность order_id, снимок balance); возврат списывает пропорционально; детализация с пагинацией + фильтр type/source |
| Управление участниками | Колонка appointment_user.member_level (миграция 000008); полный CRUD карт участника в админке (права 365-369) |
| Цепочка заказа в мини-программе | Детали услуги → подтверждение заказа (выбор купона/порог неактивен/клиентская оценка суммы) → POST /order → оплата WeChat/балансом; всего 20 страниц мини-программы |
| Замкнутый цикл групповых покупок | Повторное участие в join — 422 + блокировка при заполнении + ленивое закрытие по истечении; при формировании группы store передаёт promotion_id и оформляет по групповой цене (discount_percent), купоны/карты/баллы не суммируются, при несформированной группе заказ автоматически отменяется и освобождается блокировка мастера (старый канал FLASH_SALE отключён, распродажа идёт отдельным каналом) |
| Рабочее место управляющего | 4 интерфейса service /api/store-manager (overview/orders/technicians/revenue) с жёсткой изоляцией store_id (без филиала — 403); обзор филиала в админке + фильтр заказов по store_id + страница Flutter + право 372 |
| Дистрибьюторская комиссия | После первого заказа приглашённого со статусом completed пригласившему начисляется paid_amount × reward_rate (системная конфигурация, по умолчанию 0.05) на кошелёк (WalletTxn referral_reward); тройная идемпотентность: блокировка строк + проверка пустоты + повторная проверка первого заказа; детализация earnings + просмотр записей в админке (право 379) |
| Магазин обмена бонусов | Две таблицы: товары обмена/записи обмена; интерфейс обмена с Redis NX + блокировка строк против перевыдачи + uk_user_goods ограничивает одного пользователя одним обменом; три результата: coupon выдаёт купон / wallet зачисляет / gift_card карта; CRUD в админке + вкл/выкл + записи (права 373-378) |
| Перенос записи | POST /api/order/reschedule/{id} — смена времени у того же мастера; только pending/paid/confirmed и не менее чем за 6ч до начала услуги; order_lock + блокировка нового слота SETNX(180s) против перепродажи + проверка конфликтов расписания B2; запись в appointment_order_reschedule + подписка SCENE_RESCHEDULE |
| Передача купонов | 8-значный уникальный код передачи (uk_code как запасной, действует 7 дней); claim против злоупотреблений: Redis NX + повторная проверка блокировкой строк против двойного списания, uk_user_coupon ограничивает одну передачу, полученный купон нельзя передавать дальше, нельзя получить самому; ленивое истечение возвращает исходный купон |
| Истечение бонусов | expires_at (по умолчанию 365 дней, конфигурация points.expiry_days); PointsExpiryTimer сканирует курсором каждые 60с и списывает отрицательные значения type=expire (трёхслойная идемпотентность) + агрегированные внутрисайтовые уведомления; истёкшие бонусы нельзя использовать для оплаты/обмена |
| Автоматическое присвоение уровня мастера | TierRatingService в реальном времени считает объём заказов + средний балл и пишет в profile, подбор сверху вниз по tier_config; только повышение, без понижения (allowDowngrade для ручной переоценки); изменения в appointment_technician_tier_log + внутрисайтовое уведомление; просмотр логов в админке (право 380) |
| Замкнутый цикл распродажи | /api/seckill — акции + buy с идемпотентностью/защитой от параллелизма, при заказе подставляется seckill_id с переиспользованием store(), запас списывается блокировкой строк внутри транзакции (цена распродажи = seckill_price по данным БД), распродано — 422 «Всё разобрано», при отмене запас не возвращается; старый канал promotion flash_sale отключён |
| Напоминание перед началом услуги | ServiceReminderTimer каждые 60с сканирует заказы confirmed/serving с началом в течение 1ч → подписка SCENE_REMINDER + внутрисайтовое уведомление (защита от дублей по order_id+type, трёхслойная идемпотентность); при отсутствии шаблона — автоматически внутрисайтовое уведомление |
| Напоминание об истечении | ExpiryReminderTimer каждые 6ч сканирует карты участника/купоны с истечением в течение 3 дней → type=card_expiry/coupon_expiry + подписка SCENE_EXPIRY (order_id фиксирует источник против дублей) |
| Ответ мастера на отзыв | POST /api/technician/review/reply/{order_id}: не своя запись — 404, повторный ответ — 422, после ответа — внутрисайтовое уведомление пользователю; в appointment_order_review добавляется replied_at; детали ответа в админке (право 381) |
| Уведомление о пополнении | В транзакции колбэка пополнения WeChat пишется внутрисайтовое уведомление type='wallet_recharge' (переиспользована идемпотентность колбэка, атомарный коммит в той же транзакции, сбой не блокирует основной процесс) |
| Перевод баланса | POST /api/wallet/transfer — перевод между пользователями: 0.01-1000 за операцию + лимит 5000 в день; Redis NX + блокировка строк обоих кошельков (сортировка user_id против взаимоблокировок) + идемпотентность client_token 24ч; двойная запись WalletTxn transfer_out/transfer_in со снимком balance_after; получателю — внутрисайтовое уведомление type='balance_received' |
| Передача бонусов | POST /api/user/points/transfer — передача между пользователями: 1-10000 бонусов + накопительный лимит 10000 в день; Redis NX + lockForUpdate по последней записи обеих сторон (сортировка против взаимоблокировок) + повторная проверка внутри блокировки; двойная запись: отправитель consume/получатель earn (у получателя есть expires_at и бонусы нормально истекают); получателю — внутрисайтовое уведомление type='points_received' |
| Дополнение к отзыву | POST /api/order/review/{order_id}/append: не своя запись — 404 / повторно — 422 / пустой текст — 422 / не completed — 422, при успехе — внутрисайтовое уведомление мастеру type='review_append'; в appointment_order_review добавлены append_content/append_images(JSON)/append_at; заодно зарегистрирован маршрут отправки отзыва для пользователя (исходный store был недоступен без маршрута) и исправлен его скрытый TypeError |
| Отслеживание доставки | GET /api/order/logistics/{id}: только свои заказы product (404 — не своя запись/не товар/не отправлен); чтение order.remark JSON (shipping_company/tracking_no/shipped_at, админка пишет при отправке); маскирование телефона получателя 138****5678 |
| Настройки уведомлений | Таблица appointment_user_notify_setting (уникальный ключ uk_user_type, отсутствующая строка = всё включено по умолчанию); GET/PUT /api/user/notify-settings; 5 типов переключателей service_reminder/card_expiry/points_expiry/marketing/system (system всегда включён, отключить нельзя); notifySettingEnabled управляет 3 таймерами + событиями подписки, при выключении пропускаются и внутрисайтовые уведомления, и подписки |
| Календарь записи | GET /api/calendar/technician/{id} (месяц) + /day (день): разворачивание time_slots JSON по часовым слотам, исключение занятых слотов из appointment_order; визуальный выбор времени по расписанию филиала |
| Уровни роста пользователя | appointment_user_growth + appointment_growth_level (бронза 0/серебро 100/золото 500/платина 2000/бриллиант 5000); отметка +10, отзыв +20, за каждые 1 юань расхода 1 балл (переиспользование существующей проверки состояния даёт естественную идемпотентность); GET /api/growth (обзор/records/levels — публичные уровни) |
| Электронные счета | POST/GET /api/invoices (запрос/список/детали): uk_order_type(order_id,order_type) против повторных запросов, сумма берётся с сервера; выписка/отклонение в админке (права 382-384) |
| Тикеты поддержки | POST/GET /api/tickets + /{id}/close: создание/список/детали/закрытие пользователем; ответы в админке (права 385/387) |
| Многоуровневая дистрибуция — комиссия 2-го уровня | После оплаты заказа пригласившему пригласителя первого уровня начисляется paid×level2_rate (конфигурация 0.02): транзакция с блокировкой строк + идемпотентность uk_order_referred против повторных выплат; WalletTxn TYPE_REFERRAL_LEVEL2; просмотр записей в админке (право 386) |
| Привилегии уровней роста | Реализация GrowthLevel.benefits: скидка по уровню discount_rate при заказе (только стандартные заказы; купон/карта посещений → скидка уровня суммируется, сумма скидки в discount_amount + примечание для трассировки, нижний предел обрезается до 0); в платёжном колбэке зачисление баллов роста floor(paid×points_multiplier) с множителем (уровень фиксируется на момент оплаты, без повышения) |
| Управление реквизитами счёта | appointment_invoice_title — библиотека часто используемых реквизитов: сохранение/редактирование/удаление/по умолчанию (первая запись автоматически по умолчанию, при удалении по умолчанию передаётся другой, установка по умолчанию обнуляет остальные в транзакции); при запросе счёта можно передать title_id, ручной ввод совместим |
| Удовлетворённость тикетом | При закрытии тикета можно поставить оценку 1-5 (вне диапазона — 422, не указано — совместимо с NULL); сводка в админке: средний балл/распределение 1-5 звёзд/счётчики оценённых и неоценённых (право 388) |
| Модерация фото отзывов | admin ReviewAuditController: список отзывов с фото (фильтр JSON_LENGTH + join с именами пользователя/мастера), скрытие/восстановление (hide только для visible, restore только для hidden, двусторонняя проверка 422); после скрытия отзыв автоматически невидим в списке отзывов мастера (права 389-391) |
| История просмотров | appointment_browse_history (uk_user_item — повторный просмотр только обновляет viewed_at): запись при просмотре деталей услуги (try/catch не блокирует основной процесс, без входа пропускается); список с join данных услуги + hashid; удаление одной/очистка — только своё |

> Раунд-8 операционные исправления: устранены 12 скрытых fatal в Poster::verify; статистика DashboardController переведена на запросы Capsule Manager.
>
> Раунд-15 дополнения: возврат бонусов (при отмене/возврате возвращаются points_offset, 5 точек подключения refundOffsetPoints с идемпотентностью); статус PromotionParticipant переведён на целочисленные константы (исправлено повреждение join 1366 в строгом режиме).
>
> Раунд-16 дополнения: обмен бонусов (PointsExchangeController, тип consume/source=exchange); групповой заказ (в appointment_order добавлены колонки promotion_id/participant_id); дистрибьюторская комиссия (ReferralRewardService подключён к WorkController::complete).
>
> Раунд-17 дополнения: перенос записи (appointment_order_reschedule + интерфейс reschedule); передача купонов (appointment_user_coupon_transfer + transfer/claim/transfers); истечение бонусов (expires_at + процесс PointsExpiryTimer); автоматическое присвоение уровня мастера (TierRatingService + appointment_technician_tier_log, право 380).
>
> Раунд-17 исправление: вставка уведомлений AutoCancelTimer переведена на \support\Model::generateId() (раньше вызывался несуществующий Snowflake::generate(), уведомления об автоматической отмене молча не создавались).
>
> Раунд-18 дополнения: заказ по распродаже (store() поддерживает цену flash_sale); напоминание перед началом услуги (ServiceReminderTimer + SCENE_REMINDER); напоминание об истечении карт участника/купонов (ExpiryReminderTimer + SCENE_EXPIRY); ответ мастера на отзыв (интерфейс review reply + колонка replied_at + право 381); уведомление о пополнении (в транзакции колбэка type='wallet_recharge').
>
> Раунд-19 дополнения: перевод баланса (appointment_wallet_transfer + WalletTransferController, двойная блокировка строк с правами + идемпотентность client_token); передача бонусов (appointment_user_points_transfer + PointsTransferController, дневной лимит + двусторонние записи); дополнение к отзыву (в appointment_order_review три колонки append + интерфейс append + зарегистрирован маршрут store); отслеживание доставки (интерфейс logistics + разбор remark JSON + маскирование телефона); настройки уведомлений (appointment_user_notify_setting + NotifySettingController + управление 3 таймерами).
>
> Раунд-20 дополнения: календарь записи (CalendarController — вид месяц/день + исключение занятых слотов); уровни роста пользователя (appointment_user_growth + appointment_growth_level 5 уровней + подключение отметки/отзыва/расхода); электронные счета (appointment_invoice + uk_order_type против повторов + выписка/отклонение в админке, права 382-384); тикеты поддержки (appointment_ticket: создание/список/детали/закрытие + ответы в админке, права 385/387); многоуровневая дистрибуция — комиссия 2-го уровня (payLevel2Reward транзакция с блокировкой строк + идемпотентность uk_order_referred, право 386).
>
> Раунд-21 дополнения: реализация привилегий уровней роста (скидка discount_rate при заказе + множитель бонусов points_multiplier при оплате, миграция сид 5 уровней benefits); управление реквизитами счёта (appointment_invoice_title + связь title_id при запросе); удовлетворённость тикетом (оценка rating/rated_at при закрытии + сводная статистика в админке, право 388); модерация фото отзывов (ReviewAuditController скрытие/восстановление, права 389-391); история просмотров пользователя (appointment_browse_history + запись при просмотре деталей + список/удаление/очистка).
>
> Раунд-22 дополнения: акция «скидка при достижении суммы» (appointment_full_reduction автоматическая скидка + проверка порога, права 396-400); экспорт календаря ICS (RFC5545 мои записи); учёт рабочего времени мастера (appointment_technician_attendance отметка начала/конца дня + метка опоздания + статистика в админке, права 392-393); сервис APP-пуша (абстракция на конфигурации + подключение 5 событий, appointment_push_log); официальное разделение средств WeChat (appointment_profit_sharing_log на конфигурации + деградация, право 394); конфиденциальность (экспорт данных + удаление учётной записи через 72ч, конечный автомат close_status).
>
> Раунд-23 дополнения: медицинский профиль пользователя (appointment_user_health_profile); платёжный пароль кошелька (appointment_user_wallet pay_password установка/проверка); массовое составление расписания мастера (импорт batch + обнаружение конфликтов перекрытия); таймлайн статусов заказа (appointment_order_status_log точки записи 8 статусов + отображение в клиенте/админке); колесо удачи за бонусы (appointment_lucky_wheel + appointment_wheel_record взвешенный розыгрыш, права 401-406); срок действия бонусов (конфигурация points.expiry_days + новые записи earn с expires_at).
>
> Раунд-24 дополнения: гостевой режим (/api/guest/* чтение без входа + кэш Redis); распродажа (appointment_seckill_activity + Redis NX блокировка строк при покупке + подстановка appointment_order.seckill_id при заказе, права 407-411/420); управление версиями APP и проверка обновлений (appointment_app_version + /api/app/version, права 416-419); награда постоянным клиентам (бонус за повторную покупку в течение 30 дней type=return_customer, права 412-414); экспорт расписания в CSV (UTF-8 BOM + детализация слотов, право 415).
>
> 2026-08-26 усиление безопасности: цена позиций заказа при заказе всегда берётся из записей БД (клиентская цена недостоверна, неизвестный target_type — 422, target_id должен быть hashid), групповые/распродажные цены тоже по данным БД; запас распродажи единообразно списывается блокировкой строк внутри транзакции /api/order store() (SeckillController::buy больше не резервирует заранее, сохраняются Redis-блокировка акции + идемпотентность client_token); при запросе вывода средств мастером средства резервируются, перед одобрением перевода проводится повторная проверка, параллельное одобрение защищено от двойной выплаты; в колбэке WeChat total_fee строго сравнивается с суммой заказа, логи колбэков Alipay маскируются; после успешной установки /install пишется .install.lock — двойная защита от повторной установки; консолидированы версии зависимостей (webman-scout 2.0.5 / opensearch-php ^2.6 / точная фиксация dompdf, security-php, webman-database); в обоих приложениях phpstan.neon приведён к работоспособному состоянию (php -d memory_limit=2G).

## Навигация по документации

| Документ | Описание |
|------|------|
| [Архитектура](ARCHITECTURE.md) | Архитектура системы, связь трёх платформ, технологические компоненты, потоки данных |
| [Возможности](FEATURES.md) | Полный список функций: пользовательская часть/мастер/админка |
| [Проектирование архитектуры](ARCHITECTURE-DESIGN.md) | Слоистая архитектура, цепочка middleware, проектирование БД, проектирование безопасности |
| [Проектирование функций](FEATURE-DESIGN.md) | Ключевые бизнес-процессы, бизнес-правила, конечные автоматы, правила возврата |
| [API-документация](API.md) | Бизнес-API + API админки, с примерами запросов/ответов + OpenAPI-эндпоинтами |
| [Инструкция по установке](INSTALL.md) | Требования к окружению, развёртывание Docker, переменные окружения, сторонние конфигурации, FAQ |
| [Инструкция по использованию](USAGE.md) | Настройка админки, работа пользовательской части/мастера, правила возврата (API см. API.md) |
| [Структура проекта](STRUCTURE.md) | Полное дерево каталогов, цепочка выполнения middleware, список таблиц БД |
| [Отчёт о тестировании](TEST-REPORT.md) | Аудит покрытия тестами (558 кейсов / 2508 утверждений) |
| [Спецификация](specs/2026-05-26-appointment-system-design.md) | Спецификация проектирования системы |
| [План реализации](plans/2026-05-26-appointment-system-plan.md) | Поэтапный план реализации |

## Поддержка проекта / Support

Если этот проект вам помог — поддержите его! Спасибо за ваше участие :heart:

If this project helps you, your support is welcome and appreciated!

<table>
  <tr>
    <td align="center" width="50%">
      <img src="../weixinpay.png" alt="微信支付 / WeChat Pay" width="130" height="130"><br>
      <b>微信支付</b><br>WeChat Pay
    </td>
    <td align="center" width="50%">
      <img src="../alipay.png" alt="支付宝 / Alipay" width="130" height="130"><br>
      <b>支付宝</b><br>Alipay
    </td>
  </tr>
</table>

### Глобальные банковские переводы / Global Bank Transfer

Поддерживаются пожертвования банковским переводом из любой страны (HKD / CNY / USD / другие валюты). Спасибо за вашу щедрость :heart:

Global bank transfer donations are welcome (HKD / CNY / USD / other currencies). Thank you for your generosity!

| Позиция / Item | Информация / Details |
|-----------|-------------|
| Имя получателя / Beneficiary Name | WANG KEXUN |
| Номер счёта / Account Number | 881015918251 |
| Банк получателя / Bank | ZA Bank Limited（SWIFT: AABLHKHHXXX，банковский код / Bank Code：387） |
| Адрес банка / Bank Address | Core F, Cyberport 3, 100 Cyberport Road, Hong Kong |

> **Банк-посредник для трансграничных переводов / Intermediary Bank (if required)**
> Это информация о банке-посреднике (транзитном банке), а не о банке получателя. Уточните в своём банке, требуется ли её указывать.
> Note: this is intermediary bank information, not the receiving bank. Please check with your remitting bank whether it is required.
>
> - Для HKD / CNY / USD：**Citibank N.A. Hong Kong** — SWIFT: CITIHKHXXXX，банковский код / Bank Code：006，отделение / Branch：Hong Kong Branch，код отделения / Branch Code：391，адрес / Address：Citibank Tower, Citibank Plaza, 3 Garden Road, Central, Hong Kong
> - Для других валют (For other currencies)：**The Bank of New York Mellon** — SWIFT: IRVTUS3NXXX，адрес / Address：240 Greenwich Street, New York, United States

## Авторские права

Copyright (c) 2026 erik <erik@erik.xyz> — https://erik.xyz

### Пожертвование в криптовалюте (Crypto Donation)

Если этот проект помог вам, отсканируйте QR-код, чтобы сделать пожертвование, спасибо!

| Сеть (Network) | QR-код (QR Code) | Адрес кошелька (Wallet Address) |
|---|---|---|
| BNB Smart Chain (BEP20) | [<img src="../coin/1.jpg" width="150" alt="BNB Smart Chain (BEP20)">](../coin/1.jpg) | `0x355d429f97511897ccb4e271ec888205f9ab6629` |
| Tron (TRC20) | [<img src="../coin/2.jpg" width="150" alt="Tron (TRC20)">](../coin/2.jpg) | `TEdDHWLajt1XvqtPDWmQctdrJaC3pzZZzz` |
| Ethereum (ERC20) | [<img src="../coin/3.jpg" width="150" alt="Ethereum (ERC20)">](../coin/3.jpg) | `0x355d429f97511897ccb4e271ec888205f9ab6629` |
| Aptos | [<img src="../coin/4.jpg" width="150" alt="Aptos">](../coin/4.jpg) | `0x836e3780edfc3f7b2372b39e2a1a3a5d7adfaccd96c726f21cfde1b50dd68030` |
| Plasma | [<img src="../coin/5.jpg" width="150" alt="Plasma">](../coin/5.jpg) | `0x355d429f97511897ccb4e271ec888205f9ab6629` |
| Polygon POS | [<img src="../coin/6.jpg" width="150" alt="Polygon POS">](../coin/6.jpg) | `0x355d429f97511897ccb4e271ec888205f9ab6629` |
| Solana | [<img src="../coin/7.jpg" width="150" alt="Solana">](../coin/7.jpg) | `2hfhboHdmdrYsY25XfQSsEWxq5ip4EQsR7f4AzSRMUyr` |
| The Open Network (TON) | [<img src="../coin/8.jpg" width="150" alt="The Open Network (TON)">](../coin/8.jpg) | `UQB9kFQohzmXUir9QSSZq01iwl9aQZIDdBpNmDklljRtCoGK` |
| Arbitrum One | [<img src="../coin/9.jpg" width="150" alt="Arbitrum One">](../coin/9.jpg) | `0x355d429f97511897ccb4e271ec888205f9ab6629` |
| AVAX C-Chain | [<img src="../coin/10.jpg" width="150" alt="AVAX C-Chain">](../coin/10.jpg) | `0x355d429f97511897ccb4e271ec888205f9ab6629` |

